export const metadata = {
  title: "Prayer Board",
  description: "Share prayer requests, stand in faith with others, and support the community through active prayer.",
};

export default function PrayersLayout({ children }) {
  return children;
}
