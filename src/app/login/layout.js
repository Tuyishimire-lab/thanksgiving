export const metadata = {
  title: "Log In",
  description: "Sign in to access and sync your daily streaks, highlights, reading plan progress, and reflections.",
};

export default function LoginLayout({ children }) {
  return children;
}
