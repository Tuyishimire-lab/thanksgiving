export const metadata = {
  title: "My Profile",
  description: "View and manage your devotional streak, highlighted verses, and personal reflections.",
};

export default function ProfileLayout({ children }) {
  return children;
}
