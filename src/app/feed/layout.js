export const metadata = {
  title: "Community Feed",
  description: "Read, reflect, and share inspiring stories and testimonies of faith and gratitude from our community.",
};

export default function FeedLayout({ children }) {
  return children;
}
